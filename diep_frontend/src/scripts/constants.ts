export enum Direction {
    UP = 'ArrowUp',
    DOWN = 'ArrowDown',
    LEFT = 'ArrowLeft',
    RIGHT = 'ArrowRight'
}

export const MOVE_VALUE = 7;

export type Point = {
    x: number,
    y: number
}